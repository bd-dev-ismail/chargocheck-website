import img from '/assets/banner.png';
const Banner = () => {
    return (
        <div style={{
            // backgroundImage: `url(${img})`
        }} className="h=[60vh]">
            
        </div>
    );
};

export default Banner;